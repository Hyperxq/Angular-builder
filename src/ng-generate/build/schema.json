{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsAngularComponent",
  "title": "Angular Component Options Schema",
  "type": "object",
  "description": "Creates a new, generic component definition in the given project.",
  "additionalProperties": false,
  "properties": {
    "allowInstallCollections": {
      "type": "boolean",
      "description": "Allows to install collections when they don't already installed",
      "default": true,
      "x-prompt": "Do you want to install the collections if they are not already installed?"
    },
    "allowUnInstallCollections": {
      "type": "boolean",
      "description": "Allows to uninstall collections after the process has finished",
      "default": false,
      "x-prompt": "Do you want to uninstall the collections after the process has finished?"
    }
  }
}